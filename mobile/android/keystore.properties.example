# Copy to keystore.properties (gitignored) after generating a release
# keystore - see mobile/README.md for the keytool command. Without this
# file, release builds fall back to the debug signing key.
storeFile=app/release.keystore
storePassword=change-me
keyAlias=lab-expertgg-release
keyPassword=change-me
